#!/usr/bin/env bash
set -euo pipefail
echo "Deploying backend + frontend to STAGING..."
# replace below with your actual commands
# docker compose -f docker-compose.prod.yml up -d --build
# npm run migrate
echo "✅ Staging deployment complete"
